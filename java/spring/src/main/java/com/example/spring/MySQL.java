package com.example.spring;

//* In order to work with MySQL, you need to add the following dependencies:
//* spring-boot-starter-data-jpa, mysql-connector-j (not java, only j), spring-boot-starter-validation

//! Prior to assigning the database, you need to create it somehow in MySQL.

//* In the "application.properties" file, you need to add the following:
//* spring.datasource.url=jdbc:mysql://(host):(port)/(database_name)
//* spring.datasource.username=(username)
//* spring.datasource.password=(password)
//* spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
//* spring.jpa.hibernate.ddl-auto=update

public class MySQL {
}
